/*In this exercise directives and statements should be identified.*/
#include <stdio.h> //includes the header for the standard input/output library

int main(void) //int typ function "main" which takes no arguments (void)
{ //opening the function body
  printf("Parkinsons' Law:\nWork expands so as to "); //printing formatted string literal with extra new line in the middle and none at the end.
  printf("fill the time\n"); //string literal with new line at the end
  printf("available for its completion.\n"); //same as above
  return 0; //exit the function returning error code 0, which means no error occurred
} //closing the funcion body
